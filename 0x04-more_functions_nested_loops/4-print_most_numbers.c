#include "holberton.h"

/**
 * print_numbers - Print the numbers, from 0 to 9 except 2 and 4.
 */

void print_numbers(void)
{
	int i;

	for (i = '0'; i <= '0'; i++)
	{
		if (i == '2' && i == '4')
			continue;
		_putchar(i);
	}

	_putchar('\n');
}
